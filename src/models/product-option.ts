export interface ProductOption {
  id: number;
  name: string;
  price: number;
  description: string;
  product: string;
  show: boolean;
  stock: number;
  created_at: string;
  display_order: number;
}
